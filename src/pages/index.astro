---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";

const posts = await Astro.glob("./blog/posts/*.mdx");
posts.sort(
  (a, b) => b.frontmatter.pubDate.valueOf() - a.frontmatter.pubDate.valueOf()
);

const last_posts = posts.slice(0, 3);
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-12 mt-5">
    <div class="text-xl py-1">Hola 👋</div>
    <h1 class="text-5xl font-bold">
      Soy <span class="text-secondary">Marco Agüero</span>
    </h1>
    <div class="text-3xl py-3 font-bold">
      Ingeniero de software en formación y entusiasta de la IA
    </div>
    <div class="py-2">
      <text class="text-lg">
        Soy un desarrollador enfocado en <b>Frontend y Mobile</b>, apasionado
        por la IA y una persona curiosa a la que le encanta asumir nuevos retos.
      </text>
      <br />
      <br />
      <text class="text-lg">
        Actualmente desarrollo sitios web con <b>NextJS</b> y <b>AstroJS</b>,
        aplicaciones móviles y de escritorio con <b>React Native</b>.
        Actualmente también estoy experimentando con la biblioteca Flet de
        Python para desarrollar aplicaciones con Flutter.
      </text>
    </div>
    <div class="mt-8">
      <a
        class="btn btn-outline"
        href="https://twitter.com/nandev_16"
        target="_blank"
      >
        ¡Contáctame!
      </a>
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">Mi último proyecto {"</>"}</div>
  </div>

  <HorizontalCard
    title="Sarcasm IA"
    img="/images/projects/sarcasmai.png"
    desc="Proyecto para integrar un modelo de IA en una aplicacion web con Next.js usando streaming de datos."
    url="https://www.chevadark.club/"
    badge="NEW"
  />

  <div>
    <a class="link link-success mt-8" href="/projects"
      >Ver todos los proyectos</a
    >
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-5 mt-10">Lo último en mi blog</div>
  </div>

  {
    last_posts.map((post) => (
      <>
        <HorizontalCard
          title={post.frontmatter.title}
          img={post.frontmatter.heroImage}
          desc={post.frontmatter.description}
          url={post.url}
          target="_self"
          badge={post.frontmatter.badge}
        />
        <div class="divider my-0" />
      </>
    ))
  }
</BaseLayout>
