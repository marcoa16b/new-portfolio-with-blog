---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";

const posts = await Astro.glob("./blog/posts/*.mdx");
posts.sort(
  (a, b) => b.frontmatter.pubDate.valueOf() - a.frontmatter.pubDate.valueOf()
);

const last_posts = posts.slice(0, 3);
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-12 mt-5">
    <div class="text-xl py-1">Hola 👋</div>
    <h1 class="text-5xl font-bold">
      Soy <span class="text-secondary">Marco Agüero</span>
    </h1>
    <div class="text-3xl py-3 font-bold">
      Diseñador y desarrollador web e ingeniero en Informatica en formación
    </div>
    <div class="py-2">
      <text class="text-lg">
        Soy un diseñador y desarrollador web de Costa Rica. Actualmente esoy
        estudiando Ingeniería en Informática para complementar mis
        conocimientos. Además, soy un apasionado por la Inteligencia Artificial
        y el desarrollo de aplicaciones móviles.
      </text>
      <br />
      <br />
      <text class="text-lg">
        Actualmente desarrollo sitios web con <b>NextJS</b> y <b>AstroJS</b>.
        Ademas, para sitios web mas grandes utilizo Webflow para optimizar el
        tiempo de desarrollo.
      </text>
    </div>
    <div class="mt-8">
      <a
        class="btn btn-outline"
        href="https://www.instagram.com/nando._.dev/"
        target="_blank"
      >
        ¡Contáctame!
      </a>
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">
      Mis últimos proyectos {"</>"}
    </div>
  </div>

  <HorizontalCard
    title="Landing page de funnel de ventas para un libro de recetas saludables"
    img="/images/projects/recetas.jpg"
    desc="Un proyecto hecho para aumentar las ventas de un libro de recetas saludables."
    url="#"
    badge="Building"
  />
  <div class="divider my-0"></div>
  <HorizontalCard
    title="Control de citas para una clinica dental"
    img="https://github.com/marcoa16b/dental-system/raw/master/image.png"
    desc="El proyecto consiste en un sistema de control de citas para una clinica dental."
    url="https://github.com/marcoa16b/dental-system"
    badge="NEW"
  />

  <div>
    <a class="link link-success mt-8" href="/projects"
      >Ver todos los proyectos</a
    >
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-5 mt-10">Lo último en mi blog</div>
  </div>

  {
    last_posts.map((post) => (
      <>
        <HorizontalCard
          title={post.frontmatter.title}
          img={post.frontmatter.heroImage}
          desc={post.frontmatter.description}
          url={post.url}
          target="_self"
          badge={post.frontmatter.badge}
        />
        <div class="divider my-0" />
      </>
    ))
  }
</BaseLayout>
